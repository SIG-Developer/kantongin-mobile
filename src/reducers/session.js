import {
} from '../constants';

const initialState = {
  items: [],
  logged: false,
};

export default function (state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
